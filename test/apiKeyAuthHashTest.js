
var assert = require('assert');

describe('api key hash auth', () => {
    describe('authenticate', () => {
        it('should authenticate user by hash', (done) => {
            done()
            // process.env.DATABASE_URL = 'database url '
            // var apiKeyAuth = require('../services/auth/apiKeyHashAuth')
            //
            //
            // apiKeyAuth.authenticate('hash fdsfdsfljk').then(result => {
            //     console.log('result: ', result);
            //     done()
            // }).catch((e) => {
            //     console.log("ERROR", e)
            // })




        });
    });
});