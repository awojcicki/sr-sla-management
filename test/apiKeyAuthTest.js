
var assert = require('assert');

describe('api key auth', () => {
    describe('authenticate', () => {
        it('should authenticate new user', (done) => {
            done()
            // process.env.DATABASE_URL = 'sample database url'
            // var apiKeyAuth = require('../services/auth/apiKeyAuth')
            // apiKeyAuth.authenticate('sample key').then(result => {
            //     console.log('result: ', result);
            //     done()
            // }).catch((e) => {
            //     console.log("ERROR", e)
            // })




        });
    });
});